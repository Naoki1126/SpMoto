<% if event.participated_by?(current_user) %>
	<h4 class="event-participates-count">
		<%= link_to event_event_paricipates_path(event) do %>
			<%= "現在の参加人数: #{event.event_participates.count}" %>
		<% end %>
	</h4>
	<%= link_to  event_event_participates_path(event), method: :delete , remote:true do %>
		<span class="glyphicon glyphicon-ok-sign" aria-hidden="true" style='color:red'></span>
	<% end %>
<% else %>
	<h4 class="event-participates-count">
		<%= link_to event_event_paricipates_path(event) do %>
			<%= "現在の参加人数: #{event.event_participates.count}" %>
		<% end %>
	</h4>
	<%= link_to event_event_participates_path(event),method: :post,remote:true do %>
		<span class="glyphicon glyphicon-ok-circle" aria-hidden="true"></span>
	<% end %>
<% end %>