<div class= "notification-message">
	<%= notification_form(notification) %><span class="oderate-font"><%= "(#{time_ago_in_words(notification.created_at)}前)" %></span>
	<br>
	<% if !@comment.nil? %>
		<p class ="notification-comment"><%= @comment %></p>
	<% end %>

</div>