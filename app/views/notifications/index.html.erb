<div class="row">
	<div class="col-xs-1">
	</div>
	<div class = "col-xs-10">
		<div class= "notification-list">
			<%= link_to "通知削除", notification_path(@notifications), method: :delete ,class: "fas fa-trash",id:"notification-destroy" %>
			<% if @notifications.exists? %>
				<td><%= render @notifications %></td>

			<% else %>
				<p>通知はありません</p>

			<% end %>
		</div>
	</div>
	<div class = "col-xs-1">
	</div>
</div>

